apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: pharma-qa
  namespace: argocd
  labels:
    env: qa
    managed-by: terraform
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: pharma

  source:
    # Replace 'DPP-2026' with your GitHub username after forking zen-gitops
    repoURL: https://github.com/sravs994/zen-gitops.git
    targetRevision: HEAD
    path: envs/qa

  destination:
    server: https://kubernetes.default.svc
    namespace: qa

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
      - CreateNamespace=true
      - PrunePropagationPolicy=foreground
      - PruneLast=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m

  revisionHistoryLimit: 10
